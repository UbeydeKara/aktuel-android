import {SweetText, VStack} from "../component";

export default function Settings() {
    return(
      <VStack>
          <SweetText>Ayarlar</SweetText>
          <SweetText>Aktuel Market from Hatitech</SweetText>
      </VStack>
    );
}